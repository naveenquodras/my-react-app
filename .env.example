# Database Configuration
DB_USER=postgres
DB_HOST=localhost
DB_NAME=postgres
DB_PASSWORD=your_password_here
DB_PORT=5432

# Server Configuration
PORT=3001

# Frontend (build-time). Leave empty in Docker so the SPA calls /api on the same origin.
# VITE_API_SERVER=http://localhost:3001
